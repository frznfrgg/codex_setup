{
  "name": "rm_rf_tmp_root_itself",
  "description": "rm -rf /tmp itself (the root, not a subdir)",
  "input": {
    "tool_name": "Bash",
    "tool_input": {
      "command": "rm -rf /tmp"
    },
    "cwd": "/home/example-user/myproject"
  },
  "expect": {
    "decision": "ask",
    "rule": "rm",
    "reason_code": "rm.delete_safe_root"
  }
}
